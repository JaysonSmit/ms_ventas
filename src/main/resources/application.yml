server:
  port: 8087

spring:
  application:
    name: MS_VENTAS

  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/